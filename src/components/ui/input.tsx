/**
 * The one text-field recipe, the way buttonClass() is the one button recipe.
 *
 * Team play alone had six copies of this string and none of them had a focus
 * style — a form filled in by strangers on their own phones is exactly where
 * the keyboard user should be able to see which field they are in.
 */
export const inputClass =
  "min-h-11 rounded-[var(--radius-control)] border border-line bg-canvas px-3 text-base " +
  "focus-visible:border-brass focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-brass";
